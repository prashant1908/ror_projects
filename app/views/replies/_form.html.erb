<div id="flash" style="color:green;"></div>

<%= form_for([comment,comment.replies.build], :remote=>true) do |f| %>
 
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>